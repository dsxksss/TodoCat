import 'package:flutter/material.dart';

class TimePanel extends StatelessWidget {
  const TimePanel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("TimePanel"),
    );
  }
}
