import 'package:flutter/material.dart';

class DatePanel extends StatelessWidget {
  const DatePanel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("DatePanel"),
    );
  }
}
