name: todo_cat
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # 设备测试插件
  device_preview: 1.1.0
  # 状态管理框架
  get: 4.6.5
  # 本地数据库存储hive
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.1.11

flutter:
  uses-material-design: true
